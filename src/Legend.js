import React from 'react';
import {Button} from 'reactstrap';

const Legend = () => {
    return <div style={{textAlign:'right'}}><Button color="primary">Legend</Button></div>
};

export default Legend;
